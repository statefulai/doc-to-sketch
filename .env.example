# doc-to-sketch 飞书集成配置（可选）
#
# 脚本内置了共享应用凭证，零配置即可使用。
# 首次 fetch 飞书文档时会自动打开浏览器授权，也可以手动运行 auth 预授权。
#
# 如需使用自建飞书应用，按以下步骤操作：
#
# 1. 在 https://open.feishu.cn 创建自建应用
# 2. 开通权限：docx:document:readonly、wiki:wiki:readonly、offline_access
# 3. 在应用后台配置 OAuth 回调地址：
#
#      http://localhost:19823/callback
#
# 4. 复制本文件为 .env，取消下面两行注释并填入凭证：
#
# export FEISHU_APP_ID=your_app_id
# export FEISHU_APP_SECRET=your_app_secret
#
# 5. 在终端加载环境变量：
#
#      source .env
#
# 也可以直接 export（只对当前终端生效）：
#
#      export FEISHU_APP_ID=your_app_id
#      export FEISHU_APP_SECRET=your_app_secret
#
# 注意：脚本不会自动读取 .env 文件，必须 source .env 或 export 后再运行 fetch/auth。
